# Dependencies
node_modules/

# Environment variables
.env
.env.local
.env.*.local

# Frontend build output
dist/
build/

# Logs
*.log
npm-debug.log*

# OS/editor cruft
.DS_Store
.vscode/
.idea/

# Docker
# (data itself lives in a named volume, not a file, so nothing to ignore there —
# but if you ever bind-mount to a local folder instead, add that path here)